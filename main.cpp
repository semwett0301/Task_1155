#include <iostream>
#include <map>

using namespace std;

int main() {
    int a,b,c,d,e,f,g,h;
    cin >> a >> b >> c >> d >> e >> f >> g >> h;

    if (a + c + e + g != b + d + f + h) {
        cout << "IMPOSSIBLE";
    }
    else {

    }

    return 0;
}
